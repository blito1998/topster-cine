<h1>Em Cartaz:</h1>

{{#each filmesEmCartaz}}
<div class="card mt-2">
    <div class="card-body">
        <h3>{{dataValues.titulo}}</h3>
        <p><b>Diretor:</b> {{dataValues.diretor}}</p>
        <p><b>Ano de Laçamento:</b> {{dataValues.ano}}</p>
        <p><b>Disponibilidade:</b> {{dataValues.disponibilidade}}</p>
        <a href="/filme/{{titulo}}"><button class="btn btn-success">Mais sobre</button></a>
    </div>
</div>
{{else}}
<h5>Nenhum filme em exibição!</h5>
{{/each}}

<h1 class="mt-2">Em Breve:</h1>
{{#each filmesEmBreve}}
<div class="card">
    <div class="card-body">
        <h3>{{dataValues.titulo}}</h3>
        <p><b>Diretor:</b> {{dataValues.diretor}}</p>
        <p><b>Ano de Laçamento:</b> {{dataValues.ano}}</p>
        <p><b>Disponibilidade:</b> {{dataValues.disponibilidade}}</p>
        <a href="/filme/{{titulo}}"><button class="btn btn-success">Mais sobre</button></a>
    </div>
</div>
{{else}}
<h5>Nenhum filme...</h5>
{{/each}}
